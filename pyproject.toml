[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "base-memory-os"
version = "0.1.0"
description = "Local-first personal work memory OS"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
memory-os = "memory_os.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
